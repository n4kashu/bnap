# Manifests Directory

NFT collection metadata and content manifests.

## Purpose

- Store NFT collection manifests (JSON)
- Content hash verification data
- Collection metadata schemas
- Asset metadata templates

## Structure

- Collection manifests follow standardized JSON format
- Each collection has its own manifest file
- Content hashes link to IPFS or on-chain storage
- Metadata includes asset descriptions and properties